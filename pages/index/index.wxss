.page {
  padding: 30rpx 30rpx 60rpx;
  min-height: 100vh;
  box-sizing: border-box;
  padding-bottom: calc(60rpx + env(safe-area-inset-bottom));
}
.hero-title {
  font-size: 52rpx;
  font-weight: 600;
  color: #3b3224;
  letter-spacing: 8rpx;
}
.hero-sub {
  margin-top: 12rpx;
  font-size: 24rpx;
  color: #8a7a5f;
}
.day-strip {
  margin-top: 24rpx;
  display: flex;
  align-items: center;
  font-size: 24rpx;
  color: #6d5c42;
}
.day-dot {
  margin: 0 14rpx;
  color: #c9b28a;
}
.last-card {
  margin-top: 24rpx;
  display: flex;
  align-items: center;
  background: #fdfaf3;
  border-radius: 16rpx;
  padding: 26rpx 28rpx;
  box-shadow: 0 2rpx 10rpx rgba(90, 70, 40, 0.08);
}
.last-info {
  flex: 1;
  margin-left: 22rpx;
  margin-right: 16rpx;
}
.last-title {
  font-size: 30rpx;
  font-weight: 600;
  color: #3b3224;
}
.last-sub {
  font-size: 22rpx;
  color: #9c8a6c;
  margin-top: 6rpx;
}
.last-progress {
  margin-top: 12rpx;
  height: 8rpx;
  background: #e8ddc8;
  border-radius: 4rpx;
  overflow: hidden;
}
.last-progress-inner {
  height: 100%;
  background: #a06a48;
  border-radius: 4rpx;
}
.import-bar {
  margin-top: 22rpx;
  display: flex;
  align-items: center;
  background: #fdfaf3;
  border: 1rpx dashed #c9b28a;
  border-radius: 16rpx;
  padding: 22rpx 26rpx;
}
.import-plus {
  font-size: 36rpx;
  color: #a06a48;
  font-weight: 700;
  margin-right: 16rpx;
}
.import-text {
  flex: 1;
  font-size: 26rpx;
  color: #6d5c42;
}
.import-arrow {
  font-size: 34rpx;
  color: #c9b28a;
}
.search-box {
  margin-top: 22rpx;
  display: flex;
  align-items: center;
  background: #fdfaf3;
  border-radius: 40rpx;
  padding: 14rpx 28rpx;
  box-shadow: 0 2rpx 8rpx rgba(90, 70, 40, 0.06);
}
.search-icon {
  font-size: 26rpx;
  margin-right: 12rpx;
}
.search-input {
  flex: 1;
  font-size: 26rpx;
  color: #3b3224;
}
.quote-card {
  margin-top: 26rpx;
  background: linear-gradient(135deg, #8c5a3c, #a06a48);
  border-radius: 20rpx;
  padding: 40rpx;
  color: #fdf6e9;
  box-shadow: 0 8rpx 24rpx rgba(140, 90, 60, 0.25);
}
.quote-text {
  font-size: 34rpx;
  line-height: 1.7;
  font-family: "Songti SC", "STSong", serif;
}
.quote-src {
  margin-top: 16rpx;
  font-size: 24rpx;
  opacity: 0.85;
}
.btn-primary {
  margin-top: 28rpx;
  background: #fdf6e9;
  color: #8c5a3c;
  font-size: 30rpx;
  font-weight: 600;
  border-radius: 40rpx;
  padding: 12rpx 0;
}
.section {
  margin-top: 40rpx;
}
.section-title {
  font-size: 30rpx;
  color: #8a7a5f;
  padding: 0 8rpx 16rpx;
  letter-spacing: 4rpx;
}
.book-card {
  display: flex;
  align-items: center;
  background: #fdfaf3;
  border-radius: 16rpx;
  padding: 26rpx 28rpx;
  margin-bottom: 20rpx;
  box-shadow: 0 2rpx 10rpx rgba(90, 70, 40, 0.08);
}
.cover {
  width: 76rpx;
  height: 76rpx;
  border-radius: 14rpx;
  color: #fdf6e9;
  font-size: 36rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-right: 22rpx;
  font-family: "Songti SC", "STSong", serif;
  flex-shrink: 0;
}
.c-sishu { background: #8c5a3c; }
.c-xiushen { background: #5f7d5f; }
.c-daojia { background: #5a6a8c; }
.c-bingjia { background: #8c4a4a; }
.c-shiwen { background: #96688c; }
.c-biji { background: #5c8080; }
.c-mine { background: #a06a48; }
.c-mengxue { background: #a5852e; }
.book-main {
  flex: 1;
  margin-right: 16rpx;
}
.book-title-line {
  display: flex;
  align-items: center;
}
.book-title {
  font-size: 32rpx;
  color: #3b3224;
  font-weight: 600;
}
.tag {
  margin-left: 14rpx;
  font-size: 20rpx;
  color: #a06a48;
  border: 1rpx solid #d8c6a8;
  border-radius: 8rpx;
  padding: 2rpx 10rpx;
  background: #f6efe2;
}
.book-author {
  font-size: 22rpx;
  color: #9c8a6c;
  margin-top: 6rpx;
}
.progress {
  margin-top: 14rpx;
  display: flex;
  align-items: center;
}
.progress-bar {
  width: 160rpx;
  height: 8rpx;
  background: #e8ddc8;
  border-radius: 4rpx;
  overflow: hidden;
}
.progress-inner {
  height: 100%;
  background: #a06a48;
  border-radius: 4rpx;
}
.progress-text {
  font-size: 20rpx;
  color: #9c8a6c;
  margin-left: 12rpx;
}
.btn-read {
  background: #f3ead9;
  color: #8c5a3c;
  border: 1rpx solid #d8c6a8;
  font-size: 26rpx;
  border-radius: 30rpx;
  padding: 8rpx 24rpx;
  line-height: 1.6;
  flex-shrink: 0;
}
.footer {
  text-align: center;
  color: #b3a488;
  font-size: 22rpx;
  margin-top: 60rpx;
}
.goal-bar {
  width: 140rpx;
  height: 10rpx;
  background: #e8ddc8;
  border-radius: 5rpx;
  overflow: hidden;
  margin: 0 12rpx;
}
.goal-inner {
  height: 100%;
  background: linear-gradient(90deg, #c9a06a, #a06a48);
  border-radius: 5rpx;
  transition: width 0.3s;
}
@keyframes fadeUp {
  from { opacity: 0; transform: translateY(24rpx); }
  to { opacity: 1; transform: translateY(0); }
}
.hero {
  animation: fadeUp 0.4s ease;
}
.quote-card {
  animation: fadeUp 0.5s ease;
}
.book-card {
  transition: transform 0.1s ease;
}
.card-hover {
  transform: scale(0.98);
  opacity: 0.92;
}
.plan-bar {
  margin-top: 22rpx;
  display: flex;
  align-items: center;
  background: linear-gradient(135deg, #5f7d5f, #6f8f6f);
  border-radius: 16rpx;
  padding: 22rpx 26rpx;
  color: #f4f8f4;
  box-shadow: 0 4rpx 14rpx rgba(95, 125, 95, 0.25);
}
.plan-ico {
  font-size: 30rpx;
  margin-right: 14rpx;
}
.plan-text {
  flex: 1;
  font-size: 26rpx;
}
/* 新手引导 */
.guide-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 999;
  background: #f6efe2;
}
.guide-swiper {
  height: 100%;
}
.g-slide {
  height: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 0 70rpx 160rpx;
  box-sizing: border-box;
}
.s1 { background: linear-gradient(180deg, #f6efe2, #f3ead9); }
.s2 { background: linear-gradient(180deg, #eef3ea, #e3ecdd); }
.s3 { background: linear-gradient(180deg, #f7efe2, #f3e6d2); }
.s4 { background: linear-gradient(180deg, #eef1f6, #e2e8f2); }
.g-logo {
  width: 150rpx;
  height: 150rpx;
  border-radius: 50%;
  background: linear-gradient(135deg, #8c5a3c, #a06a48);
  color: #fdf6e9;
  font-size: 76rpx;
  font-family: "Songti SC", "STSong", serif;
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 10rpx 30rpx rgba(140, 90, 60, 0.3);
  margin-bottom: 40rpx;
}
.g-ico {
  font-size: 130rpx;
  margin-bottom: 40rpx;
}
.g-title {
  font-size: 46rpx;
  font-weight: 700;
  color: #3b3224;
  letter-spacing: 4rpx;
}
.g-sub {
  margin-top: 18rpx;
  font-size: 26rpx;
  color: #8a7a5f;
}
.g-desc {
  margin-top: 50rpx;
  font-size: 28rpx;
  color: #5a4c35;
  line-height: 1.9;
  text-align: center;
  white-space: pre-line;
}
.g-dots {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 200rpx;
  display: flex;
  justify-content: center;
}
.g-dot {
  width: 14rpx;
  height: 14rpx;
  border-radius: 7rpx;
  background: #d8c6a8;
  margin: 0 8rpx;
  transition: all 0.25s;
}
.g-dot.on {
  width: 40rpx;
  background: #8c5a3c;
}
.g-skip {
  position: fixed;
  top: calc(44px + 30rpx);
  right: 40rpx;
  z-index: 1000;
  font-size: 26rpx;
  color: #9c8a6c;
  background: rgba(253, 250, 243, 0.85);
  border-radius: 30rpx;
  padding: 10rpx 28rpx;
}
.g-next {
  position: fixed;
  left: 60rpx;
  right: 60rpx;
  bottom: calc(70rpx + env(safe-area-inset-bottom));
  z-index: 1000;
  background: #8c5a3c;
  color: #fdf6e9;
  font-size: 32rpx;
  font-weight: 600;
  text-align: center;
  border-radius: 44rpx;
  padding: 20rpx 0;
  box-shadow: 0 8rpx 20rpx rgba(140, 90, 60, 0.3);
}